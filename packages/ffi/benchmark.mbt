///|
pub fn bench_ffi_generate(iterations : Int) -> String {
  let input = "{\"scale\":\"C major\",\"rhythm_kind\":\"FourQuarters\"}"
  let mut total = 0
  for i = 0; i < iterations; i = i + 1 {
    total = total + ffi_generate_melody_json(input).length()
  }
  "ffi generated bytes: \{total}"
}
